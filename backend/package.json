{
  "name": "sna3ti-ma-backend",
  "version": "1.0.0",
  "description": "Sna3ti.ma backend foundation. Express + PostgreSQL (Prisma) REST API designed to replace the Sna3tiData mock facade: auth (JWT + bcrypt), professional marketplace, verification, subscriptions, Moroccan bank-transfer payments, reviews, reports, notifications, legal content and audit. Does NOT change any frontend file.",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "cross-env NODE_ENV=test node --test tests/*.test.js",
    "prisma:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "node prisma/seed.js"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "prisma": "^5.16.1"
  }
}
